/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { YearsPipe } from './years.pipe';

describe('Pipe: Yearse', () => {
  it('create an instance', () => {
    let pipe = new YearsPipe();
    expect(pipe).toBeTruthy();
  });
});
